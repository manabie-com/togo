package tasks

type Operation struct {
	// Put store interface here
}

// Create the task for the user
func (o *Operation) Create(userID int64, task string) error {
	// Get user object

	// Check if can still create tasks

	// Insert task
	return nil
}
