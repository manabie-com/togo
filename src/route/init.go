package route

import (
	"github.com/gofiber/fiber/v2"
)

type Route struct {
}

func Init(app *fiber.App) {

	// v1 := app.Group("/api/v1")
	// v1.Get("/healthcheck/status")

	// v1.Post("/register")
	// v1.Post("/login")

	// v1.Post("/todo")
}
