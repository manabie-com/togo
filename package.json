{
  "name": "todo-go",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gotest": "cd backend && go test ./... && cd ../"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.2.13",
    "@serverless-stack/resources": "^1.2.13",
    "@tsconfig/node16": "^1.0.2",
    "@types/node": "^17.0.38",
    "aws-cdk-lib": "2.24.0",
    "typescript": "^4.7.2",
    "vitest": "^0.13.1"
  }
}
