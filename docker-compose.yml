version: '3.4'

services:
  mssqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
  mytodo.backendapi:
    image: ${DOCKER_REGISTRY-}mytodobackendapi
    build:
      context: .
      dockerfile: src/MyTodo.BackendApi/Dockerfile
#volumes:
#   sqlserver_data: